# email-campaign

## Requirements

This task implements the email-campaign feature. The implementation should satisfy all requirements defined in this task.

For `task_config.json`, it must have a non empty "needed_mcp_servers" field and a non empty "needed_local_tools" with "claim_done" in it.
